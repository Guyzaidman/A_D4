public class ExtremeDevice extends Device{

    public ExtremeDevice(int ID, String name, ePark park, int minAge, int minHeight, int minWeight) {
        super(ID, name, park, minAge, minHeight, minWeight);
    }
}
